NODE_ENV=development
APP_URL=http://localhost:4000
PORT=4000
SENTRY_DSN=

###
## Database
###
# Option 1: Use DATABASE_URL (recommended for production and CI/CD)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/postgres

# Option 2: Use individual connection parameters
POSTGRES_HOST=db
# port 54322 if using local supabase.
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
# changing the db name from postgres doesn't allow 
# the local supabase dashboard access to the db
POSTGRES_DB=postgres

###
## Supabase config
###
SUPABASE_URL=https://example.supabase.co

# Supabase Keys (2025+ format)
# Get from: supabase status
# - Publishable key: For auth operations (getClaims, signIn, signUp)
# - Secret key: For admin operations (auth.admin.*)
SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
SUPABASE_SECRET_KEY=sb_secret_xxx

# Legacy keys (deprecated - still supported for backward compatibility)
# SUPABASE_PK=example-key
# SUPABASE_ANON_KEY=eyJhbG...
# SUPABASE_AUTH_JWT_SECRET=abcdefghijklmnopqrstuvwxzyz1234567890

###
# Feature Flags
###
FEATURE_FLAG_SENTRY_DEVELOPMENT=true
